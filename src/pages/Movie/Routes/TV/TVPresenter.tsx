import React from 'react'

function TVPresenter(props: any) {
  const { nowPlaying, upcoming, popular, error, loading } = props
  return <div>HomePresenter HomePresenter</div>
}

export default TVPresenter
